#include "main.h"

/**
* _islower - function checks if character is lower
* @c: character to be evaluated
* Return: 1 if upper, 0 if lower
*/
int _islower(int c)
{

if (c >= 'A' && c <= 'B')
{
return (0);
}
else
{
return (1);
}

}
